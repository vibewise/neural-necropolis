{
  "name": "@neural-necropolis/dashboard-static",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Standalone static host wrapper for the Neural Necropolis dashboard.",
  "scripts": {
    "sync": "node ./sync.mjs",
    "serve": "npm run sync && node ./server.mjs",
    "smoke": "npm run sync && node ./smoke.mjs"
  }
}
